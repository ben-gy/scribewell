{
  "name": "scribewell",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Private in-browser transcription — audio & video to text, subtitles, and searchable transcripts, with zero uploads.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.6",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
